@model List<GradeNet.Infrastructure.ViewModels.LessonViewModel>

<style>
    .content {
        border: 1px solid #aaa;
        padding: 15px;
    }

    hr {
        width: 95%;
    }

    .select {
        width: 100%;
        background: #fff4b911;
        border-style: none;
        border-bottom: 1px solid black;
    }
</style>

<div class="container" style="padding: 20px 20%;">
    <div class="row">
        <div class="col-md-12 content">
            <h4>Wybór lekcji</h4>
            <hr>
            <form action="Lesson" method="get">
                <div class="row" style="margin-bottom:10px;">
                    <div class="col-md-1">
                        Lekcja:
                    </div>
                    <div class="col-md-3">
                        <select class="select" name="lessonId">
                            @foreach (var ls in Model)
                            {
                                <option value="@ls.LessonId">@ls.LessonName</option>
                            }
                        </select>
                    </div>
                </div>
                <div class="row">
                    <div class="col-md-12">
                        <input type="submit" value="Przejdź" />
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>